# DBMS (Database Management System)

## DBMS কী?
DBMS (Database Management System) হলো এমন একটি সফটওয়্যার সিস্টেম, যার মাধ্যমে **ডেটা সংরক্ষণ (store), ব্যবস্থাপনা (manage), আপডেট (update) এবং নিরাপদভাবে ব্যবহার (retrieve)** করা যায়।

সহজ ভাষায় বললে—  
**DBMS হলো ডেটা রাখার, খোঁজার ও নিয়ন্ত্রণ করার একটি বুদ্ধিমান ব্যবস্থা।**

---

## DBMS ব্যবহার করার প্রয়োজন কেন?

নিচে ধাপে ধাপে ব্যাখ্যা করা হলো কেন DBMS ব্যবহার করা হয়:

### 1. ডেটা সুশৃঙ্খলভাবে সংরক্ষণ
DBMS ডেটাকে **টেবিল (Table), সারি (Row) ও কলাম (Column)** আকারে সাজিয়ে রাখে,  
যার ফলে ডেটা বোঝা ও ব্যবহার করা সহজ হয়।

---

### 2. ডেটা ডুপ্লিকেশন (Redundancy) কমায়
একই ডেটা বারবার সংরক্ষণ করলে সমস্যা হয়।  
DBMS **Normalization** ব্যবহার করে ডুপ্লিকেশন কমায়।

**ফলাফল:**  
- কম স্টোরেজ খরচ  
- ডেটা অসামঞ্জস্য (Inconsistency) কমে

---

### 3. ডেটা নিরাপত্তা (Security)
DBMS–এ  
- ইউজার রোল  
- পাসওয়ার্ড  
- অ্যাক্সেস কন্ট্রোল  

ব্যবহার করে নির্দিষ্ট মানুষকে নির্দিষ্ট ডেটার অনুমতি দেওয়া যায়।

---

### 4. একাধিক ইউজার একসাথে কাজ করতে পারে
একই ডেটাবেসে অনেক ইউজার একসাথে কাজ করলেও ডেটা নষ্ট হয় না।  
এটি সম্ভব হয় **Concurrency Control** এর মাধ্যমে।

---

### 5. ডেটা দ্রুত খুঁজে পাওয়া যায়
SQL Query ব্যবহার করে খুব দ্রুত ডেটা বের করা যায়।

```sql
SELECT * FROM students WHERE dept = 'CSE';

---
```
### 6. ডেটা হারানোর ঝুঁকি কম

DBMS–এ ডেটা সুরক্ষার জন্য বিশেষ ব্যবস্থা থাকে, যার ফলে হঠাৎ কোনো সমস্যা হলেও ডেটা স্থায়ীভাবে নষ্ট হয় না।

### 🔹 Backup System
- নিয়মিতভাবে ডেটার কপি সংরক্ষণ করা হয়  
- হার্ডডিস্ক নষ্ট, ভুলে ডেটা ডিলিট বা সিস্টেম ফেইল হলেও আগের ডেটা ফেরত আনা যায়  

### 🔹 Recovery System
- সিস্টেম ক্র্যাশ বা পাওয়ার ফেইল হলেও ডেটাবেস স্বাভাবিক অবস্থায় ফিরিয়ে আনে  
- **Transaction Log** ব্যবহার করে অসম্পূর্ণ কাজ ঠিকভাবে রোলব্যাক বা কমপ্লিট করা হয়  

### 📌 ফলাফল
- ডেটা লসের ঝুঁকি অনেক কমে  
- সিস্টেমের উপর ব্যবহারকারীর বিশ্বাস বৃদ্ধি পায়  

> **অতএব, Backup ও Recovery System থাকার কারণে DBMS ডেটা হারানোর ঝুঁকি উল্লেখযোগ্যভাবে কমিয়ে দেয়।**

---

## Types of Database (ডেটাবেসের প্রকারভেদ)

ডেটা সংরক্ষণের গঠন, ব্যবহার এবং প্রযুক্তির উপর ভিত্তি করে ডেটাবেসকে বিভিন্ন প্রকারে ভাগ করা যায়। নিচে গুরুত্বপূর্ণ ডেটাবেসের প্রকারভেদ সহজভাবে ব্যাখ্যা করা হলো।

---

### 1. Hierarchical Database
এই ডেটাবেসে ডেটা **Parent–Child** কাঠামোতে সংরক্ষণ করা হয়।

#### বৈশিষ্ট্য
- একটি Parent–এর একাধিক Child থাকতে পারে  
- Tree structure অনুসরণ করে  
- One-to-Many সম্পর্ক সমর্থন করে  

#### উদাহরণ
- ফাইল সিস্টেম  
- XML ডেটা  

---

### 2. Network Database
এখানে ডেটা **নেটওয়ার্ক কাঠামোতে** সংরক্ষণ করা হয়।

#### বৈশিষ্ট্য
- Many-to-Many সম্পর্ক সমর্থন করে  
- Hierarchical Database–এর উন্নত রূপ  

---

### 3. Relational Database
ডেটা **Table (Row ও Column)** আকারে সংরক্ষণ করা হয়।

#### বৈশিষ্ট্য
- SQL ব্যবহার করে ডেটা পরিচালনা করা হয়  
- Primary Key ও Foreign Key ব্যবহার হয়  
- সবচেয়ে বেশি ব্যবহৃত ডেটাবেস  

#### উদাহরণ
- MySQL  
- PostgreSQL  
- Oracle  

---

### 4. Object-Oriented Database
ডেটা **Object** আকারে সংরক্ষণ করা হয়।

#### বৈশিষ্ট্য
- Object, Class, Inheritance সাপোর্ট করে  
- জটিল ডেটা স্ট্রাকচারের জন্য উপযোগী  

---

### 5. Centralized Database
সব ডেটা **একটি কেন্দ্রীয় স্থানে** সংরক্ষণ করা হয়।

#### বৈশিষ্ট্য
- নিয়ন্ত্রণ সহজ  
- এক জায়গায় ডেটা ম্যানেজ করা হয়  

---

### 6. Distributed Database
ডেটা **একাধিক স্থানে বিভক্তভাবে** সংরক্ষণ করা হয়।

#### বৈশিষ্ট্য
- দ্রুত ডেটা অ্যাক্সেস  
- Fault Tolerance বেশি  

---

### 7. NoSQL Database
এই ডেটাবেসে **Fixed Schema নেই**।

#### বৈশিষ্ট্য
- বড় ডেটা (Big Data) হ্যান্ডেল করতে সক্ষম  
- স্কেল করা সহজ  

#### উদাহরণ
- MongoDB  
- Cassandra  
- Firebase  

---

### 8. Cloud Database
ডেটাবেস ক্লাউড প্ল্যাটফর্মে হোস্ট করা হয়।

#### বৈশিষ্ট্য
- ইন্টারনেটের মাধ্যমে অ্যাক্সেসযোগ্য  
- Maintenance কম  

#### উদাহরণ
- Amazon RDS  
- Google Cloud Database  

---

## সংক্ষেপে বললে
> **ডেটাবেসের ধরন নির্ভর করে ডেটা স্ট্রাকচার, ব্যবহারের ধরন এবং প্রযুক্তির উপর।**
